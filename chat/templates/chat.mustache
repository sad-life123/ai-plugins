{{!
    /ai/placement/chat/templates/chat.mustache
}}
<div id="coursechat-{{uniqid}}" class="coursechat-container">
    
    <div class="coursechat-header">
        <h3>
            💬 {{#str}}chat_title, aiplacement_chat{{/str}}
            <span class="coursechat-context-badge">
                {{#str}}context_info, aiplacement_chat{{/str}}
            </span>
        </h3>
        <div>
            <button class="btn btn-sm btn-outline-secondary" onclick="M.util.js_pending('coursechat-clear'); 
                require(['aiplacement_chat/chat'], function(Chat) { 
                    Chat.init('coursechat-{{uniqid}}', {{config}}).clearHistory(); 
                    M.util.js_complete('coursechat-clear');
                }); return false;" 
                title="{{#str}}clear_history, aiplacement_chat{{/str}}">
                🗑️
            </button>
            <button class="btn btn-sm btn-outline-secondary" onclick="
                document.querySelector('.coursechat-container').remove();
                document.getElementById('coursechat-widget-btn')?.style.setProperty('display', 'flex', 'important');
                return false;">
                ✕
            </button>
        </div>
    </div>
    
    <div class="coursechat-messages">
        <!-- Приветственное сообщение будет добавлено JS -->
    </div>
    
    <div class="coursechat-typing" style="display: none;">
        <span>{{#str}}typing, aiplacement_chat{{/str}}</span>
        <div class="typing-dots">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>
    
    <div class="coursechat-input-area">
        <textarea 
            class="coursechat-input" 
            rows="1"
            placeholder="{{#str}}input_placeholder, aiplacement_chat{{/str}}"></textarea>
        <button class="coursechat-send" disabled>
            ➤
        </button>
    </div>
    
</div>