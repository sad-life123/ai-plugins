{{!
    /ai/placement/chat/templates/actions.mustache
}}
{{#actions}}
<div class="aiplacement-chat-action" data-action="{{name}}">
    <button class="btn btn-primary">
        <i class="icon fa {{icon}}"></i>
        {{title}}
    </button>
</div>
{{/actions}}
